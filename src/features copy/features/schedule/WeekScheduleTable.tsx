import React from "react";
import { WeekSchedule } from "../../../types/types.ts";

const WeekScheduleTable = (week: WeekSchedule) => {
    return(
        <div>Unimplemented</div>
    );
}

export default WeekScheduleTable;